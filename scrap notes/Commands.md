

## git

show the commit(?) tree
```git
git log --oneline --graph --decorate --all
```

### branch
```git
git branch <new branch>
git checkout <branch>
```

delete
```git
git branch --delete <branch>
```
