__row["c3"]
行号是：[
__index
]
可以是表达式
[
__index + 2
,
__f"key{index+1}"
,
__sub(f"key{index}")
,
__sub(f"key{index-1}", "file1")
]
第一列是：[
__row.iloc[0]
]
修改方括号内的数字。
第c2列是：[
__row["c2"]
]
,
[
__lastrow["c4"]
, 
__row["c4"] or lastrow["c4"]
]   
lastrow默认在空格子时不更新，见funcs.py文件

__"" and "一定要加注释的话给一个返回值为空字符的表达式，修改引号内的内容。"

一些文字123123,注意这行后面有空格，会被原样print出来   
__"\n"
// 两个下划线开始的行是特殊处理，跟别的文字会报错，其他都是按照string处理，比如上面那个就是换行
——"\n// 中文的破折号也行"
